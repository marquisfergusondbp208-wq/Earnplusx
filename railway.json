{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install --upgrade pip && pip install --force-reinstall -r requirements.txt"
  },
  "start": {
    "cmd": "python earnplusd.py"
  }
}
